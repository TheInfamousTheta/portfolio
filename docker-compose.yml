services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kaustubh-portfolio
    ports:
      - "6000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
